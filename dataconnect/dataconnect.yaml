specVersion: "v1beta"
serviceId: "progressor"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "progressor-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
